// Package publishcheck was a temporary, throwaway verification harness used
// while fixing the publish/snapshot ordering bug. It is intentionally left
// empty so the directory does not compile into a stray binary; delete the
// whole cmd/publishcheck directory if it is not needed.
package publishcheck
